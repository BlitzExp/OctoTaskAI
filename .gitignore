target/
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

# Local environment & secrets — never commit
.env
.env.local
wallet/
!wallet/.gitkeep

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Terraform — never commit state or vars (may contain secrets)
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
